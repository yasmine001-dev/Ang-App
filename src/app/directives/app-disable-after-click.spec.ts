import { AppDisableAfterClick } from '../app-disable-after-click';

describe('AppDisableAfterClick', () => {
  it('should create an instance', () => {
    const directive = new AppDisableAfterClick();
    expect(directive).toBeTruthy();
  });
});
